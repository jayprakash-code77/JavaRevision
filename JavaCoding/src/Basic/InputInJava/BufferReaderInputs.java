package Basic.InputInJava;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader; // Usually needed to wrap System.in

public class BufferReaderInputs {
    public static void main(String []args) throws Exception {


        /*
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        try {
            String name = br.readLine();
            System.out.println("Name: "+name);
        } catch (IOException e) {
            System.out.println("An error occured while reading the inputs.");
            e.printStackTrace();
        }

         */






        /*
        // BufferedReadrer always returns a String, we have to parse it to different data types.
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String ageStr = br.readLine();
        int ageInt = Integer.parseInt(ageStr); // parsing the String to make it integer.

        System.out.println("Age: "+ageInt);

         */









    }
}
